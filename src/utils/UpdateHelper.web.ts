// utils/UpdateHelper.web.ts
export function checarAtualizacao() {
  // Este arquivo é carregado exclusivamente quando o Expo tenta gerar a versão Web.
  // Como ele não importa a biblioteca nativa, o erro "Web Bundling failed" não acontece!
  console.log("Verificação de atualização ignorada no ambiente Web.");
}
